{
  "name": "ramonas-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve public",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
